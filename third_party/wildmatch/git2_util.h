#pragma once
// iiLocalLLM build adapter. The vendored wildmatch function uses only these
// standard C declarations; no Git repository, transport or utility runtime is linked.
#include <ctype.h>
#include <string.h>
