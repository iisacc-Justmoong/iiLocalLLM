cmake_minimum_required(VERSION 3.24)
project(iiLocalLLMConsumer LANGUAGES CXX)

find_package(iiLocalLLM 0.33.0 CONFIG REQUIRED)
option(IILOCALLLM_TEST_AGENT_TOOL_GRAMMAR "Constrain catalog qualification tool generation with the upstream schema grammar" ON)
option(IILOCALLLM_TEST_AGENT_THINKING_CONTROL "Also qualify catalog discovery with thinking explicitly disabled" OFF)
add_executable(iiLocalLLMConsumer ../hello_world.cpp)
target_link_libraries(iiLocalLLMConsumer PRIVATE iiLocalLLM::iiLocalLLM)
set_target_properties(iiLocalLLMConsumer PROPERTIES CXX_EXTENSIONS OFF)
enable_testing()
find_package(Qt6 6.8.3 EXACT REQUIRED COMPONENTS Test)
foreach(_kind permission_rules skill_permission permission_settings working_directories command_hooks async_hooks http_hooks model_hooks agent_hooks mcp_output_hooks input_lifecycle session_end session_clear permission_request permission_updates permission_requests input_queue subagent shell_tasks mcp_server http mcp_http_server)
    if((_kind STREQUAL "command_hooks" OR _kind STREQUAL "async_hooks") AND (NOT UNIX OR IOS OR ANDROID OR EMSCRIPTEN))
        continue()
    endif()
    add_executable(iiLocalLLM_${_kind}_consumer ../${_kind}_tests.cpp)
    target_link_libraries(iiLocalLLM_${_kind}_consumer PRIVATE iiLocalLLM::iiLocalLLM Qt6::Test)
    set_target_properties(iiLocalLLM_${_kind}_consumer PROPERTIES AUTOMOC ON)
    add_test(NAME iiLocalLLM.installed_${_kind} COMMAND iiLocalLLM_${_kind}_consumer)
endforeach()
find_package(Python3 COMPONENTS Interpreter REQUIRED)
add_executable(iiLocalLLMHttpHookProbeConsumer ../http_hook_probe.cpp)
target_link_libraries(iiLocalLLMHttpHookProbeConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_executable(iiLocalLLMNativeSchemaConsumer ../native_schema_smoke.cpp)
target_link_libraries(iiLocalLLMNativeSchemaConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_executable(iiLocalLLMMcpOutputHookConsumer ../agent_runtime_smoke.cpp)
target_link_libraries(iiLocalLLMMcpOutputHookConsumer PRIVATE iiLocalLLM::iiLocalLLM)
if(UNIX AND NOT IOS AND NOT ANDROID AND NOT EMSCRIPTEN)
    add_executable(iiLocalLLMAsyncHookConsumer ../async_hook_runtime_smoke.cpp)
    target_link_libraries(iiLocalLLMAsyncHookConsumer PRIVATE iiLocalLLM::iiLocalLLM)
endif()
add_test(NAME iiLocalLLM.installed_http_hooks_tls COMMAND ${Python3_EXECUTABLE} -B
    ${CMAKE_CURRENT_SOURCE_DIR}/../http_hooks_tls.py $<TARGET_FILE:iiLocalLLMHttpHookProbeConsumer>)
set_tests_properties(iiLocalLLM.installed_http_hooks_tls PROPERTIES TIMEOUT 90)
add_executable(iiLocalLLMSubagentsConsumer subagents.cpp)
target_link_libraries(iiLocalLLMSubagentsConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_subagents COMMAND iiLocalLLMSubagentsConsumer)
add_executable(iiLocalLLMSubagentsInferenceConsumer ../subagent_runtime_smoke.cpp)
target_link_libraries(iiLocalLLMSubagentsInferenceConsumer PRIVATE iiLocalLLM::iiLocalLLM)
if(IILOCALLLM_TEST_AGENT_CATALOG)
    add_test(NAME iiLocalLLM.installed_subagents_inference COMMAND iiLocalLLMSubagentsInferenceConsumer "${IILOCALLLM_TEST_AGENT_CATALOG}" "${IILOCALLLM_TEST_AGENT_MODEL}")
    set_tests_properties(iiLocalLLM.installed_subagents_inference PROPERTIES TIMEOUT 600)
endif()
add_executable(iiLocalLLMSkillsConsumer skills.cpp)
target_link_libraries(iiLocalLLMSkillsConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_skills COMMAND iiLocalLLMSkillsConsumer)
add_executable(iiLocalLLMInputsConsumer inputs.cpp)
target_link_libraries(iiLocalLLMInputsConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_inputs COMMAND iiLocalLLMInputsConsumer)
add_executable(iiLocalLLMTasksConsumer tasks.cpp)
target_link_libraries(iiLocalLLMTasksConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_tasks COMMAND iiLocalLLMTasksConsumer)
if(UNIX AND NOT IOS AND NOT ANDROID AND NOT EMSCRIPTEN)
    add_executable(iiLocalLLMShellsConsumer shells.cpp)
    target_link_libraries(iiLocalLLMShellsConsumer PRIVATE iiLocalLLM::iiLocalLLM)
    add_test(NAME iiLocalLLM.installed_shells COMMAND iiLocalLLMShellsConsumer)
    add_executable(iiLocalLLMLocalApplicationsConsumer local_applications.cpp)
    target_link_libraries(iiLocalLLMLocalApplicationsConsumer PRIVATE iiLocalLLM::iiLocalLLM)
    add_test(NAME iiLocalLLM.installed_local_applications COMMAND iiLocalLLMLocalApplicationsConsumer)
    set_tests_properties(iiLocalLLM.installed_local_applications PROPERTIES TIMEOUT 30)
endif()
add_executable(iiLocalLLMDiscoveryConsumer discovery.cpp)
target_link_libraries(iiLocalLLMDiscoveryConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_discovery_consumer COMMAND iiLocalLLMDiscoveryConsumer)
add_test(NAME iiLocalLLM.installed_consumer COMMAND iiLocalLLMConsumer)
add_executable(iiLocalLLMServiceConsumer consumer.cpp)
target_link_libraries(iiLocalLLMServiceConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_service_consumer COMMAND iiLocalLLMServiceConsumer)
add_executable(iiLocalLLMAgentConsumer agent.cpp)
target_link_libraries(iiLocalLLMAgentConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_agent_consumer COMMAND iiLocalLLMAgentConsumer)
add_executable(iiLocalLLMMcpServerConsumer mcp_server.cpp)
target_link_libraries(iiLocalLLMMcpServerConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_mcp_server_consumer COMMAND iiLocalLLMMcpServerConsumer)
add_executable(iiLocalLLMMcpHttpServerConsumer mcp_http_server.cpp)
target_link_libraries(iiLocalLLMMcpHttpServerConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_mcp_http_server_consumer COMMAND iiLocalLLMMcpHttpServerConsumer)
add_executable(iiLocalLLMAgentApiConsumer api.cpp)
target_link_libraries(iiLocalLLMAgentApiConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_agent_api_consumer COMMAND iiLocalLLMAgentApiConsumer)
add_executable(iiLocalLLMCompactionConsumer compaction.cpp)
target_link_libraries(iiLocalLLMCompactionConsumer PRIVATE iiLocalLLM::iiLocalLLM)
add_test(NAME iiLocalLLM.installed_compaction_consumer COMMAND iiLocalLLMCompactionConsumer)
if(IILOCALLLM_TEST_CHAT_GGUF OR IILOCALLLM_TEST_AGENT_CATALOG)
    add_executable(iiLocalLLMTaskInferenceConsumer ../task_runtime_smoke.cpp)
    target_link_libraries(iiLocalLLMTaskInferenceConsumer PRIVATE iiLocalLLM::iiLocalLLM)
    if(IILOCALLLM_TEST_CHAT_GGUF)
        add_test(NAME iiLocalLLM.installed_tasks_inference COMMAND iiLocalLLMTaskInferenceConsumer "${IILOCALLLM_TEST_CHAT_GGUF}")
        set_tests_properties(iiLocalLLM.installed_tasks_inference PROPERTIES TIMEOUT 180)
    endif()
    if(IILOCALLLM_TEST_AGENT_CATALOG)
        if(NOT IILOCALLLM_TEST_AGENT_MODEL MATCHES "^model://")
            message(FATAL_ERROR "Agent catalog tests require a model:// URI")
        endif()
        set(_task_profile_args)
        if(IILOCALLLM_TEST_AGENT_NO_THINK)
            list(APPEND _task_profile_args --no-think)
        endif()
        if(NOT IILOCALLLM_TEST_AGENT_TOOL_GRAMMAR)
            list(APPEND _task_profile_args --no-tool-grammar)
        endif()
        add_test(NAME iiLocalLLM.installed_tasks_catalog COMMAND iiLocalLLMTaskInferenceConsumer --catalog "${IILOCALLLM_TEST_AGENT_CATALOG}" "${IILOCALLLM_TEST_AGENT_MODEL}" ${_task_profile_args})
        add_test(NAME iiLocalLLM.installed_tasks_discovery COMMAND iiLocalLLMTaskInferenceConsumer --catalog "${IILOCALLLM_TEST_AGENT_CATALOG}" "${IILOCALLLM_TEST_AGENT_MODEL}" --deferred ${_task_profile_args})
        set_tests_properties(iiLocalLLM.installed_tasks_catalog iiLocalLLM.installed_tasks_discovery PROPERTIES TIMEOUT 600)
        if(IILOCALLLM_TEST_AGENT_THINKING_CONTROL)
            add_test(NAME iiLocalLLM.installed_tasks_thinking_control COMMAND iiLocalLLMTaskInferenceConsumer --catalog "${IILOCALLLM_TEST_AGENT_CATALOG}" "${IILOCALLLM_TEST_AGENT_MODEL}" --deferred --disable-thinking ${_task_profile_args})
            set_tests_properties(iiLocalLLM.installed_tasks_thinking_control PROPERTIES TIMEOUT 600)
        endif()
        if(UNIX AND NOT IOS AND NOT ANDROID AND NOT EMSCRIPTEN)
            add_executable(iiLocalLLMInputQueueInferenceConsumer ../input_queue_runtime_smoke.cpp)
            target_link_libraries(iiLocalLLMInputQueueInferenceConsumer PRIVATE iiLocalLLM::iiLocalLLM)
            add_test(NAME iiLocalLLM.installed_input_queue_inference COMMAND iiLocalLLMInputQueueInferenceConsumer --catalog "${IILOCALLLM_TEST_AGENT_CATALOG}" "${IILOCALLLM_TEST_AGENT_MODEL}")
            set_tests_properties(iiLocalLLM.installed_input_queue_inference PROPERTIES TIMEOUT 600)
            add_executable(iiLocalLLMShellInferenceConsumer ../shell_runtime_smoke.cpp)
            target_link_libraries(iiLocalLLMShellInferenceConsumer PRIVATE iiLocalLLM::iiLocalLLM)
            add_test(NAME iiLocalLLM.installed_shell_inference COMMAND iiLocalLLMShellInferenceConsumer --catalog "${IILOCALLLM_TEST_AGENT_CATALOG}" "${IILOCALLLM_TEST_AGENT_MODEL}" ${_task_profile_args})
            set_tests_properties(iiLocalLLM.installed_shell_inference PROPERTIES TIMEOUT 600)
        endif()
    endif()
endif()
if(IILOCALLLM_TEST_CHAT_GGUF OR IILOCALLLM_TEST_AGENT_CATALOG)
    add_executable(iiLocalLLMSkillsInferenceConsumer ../skill_runtime_smoke.cpp)
    target_link_libraries(iiLocalLLMSkillsInferenceConsumer PRIVATE iiLocalLLM::iiLocalLLM)
    if(IILOCALLLM_TEST_CHAT_GGUF)
        add_test(NAME iiLocalLLM.installed_skills_inference COMMAND iiLocalLLMSkillsInferenceConsumer "${IILOCALLLM_TEST_CHAT_GGUF}")
        set_tests_properties(iiLocalLLM.installed_skills_inference PROPERTIES TIMEOUT 240)
    endif()
    if(IILOCALLLM_TEST_AGENT_CATALOG)
        add_test(NAME iiLocalLLM.installed_skills_catalog_inference COMMAND iiLocalLLMSkillsInferenceConsumer --catalog "${IILOCALLLM_TEST_AGENT_CATALOG}" "${IILOCALLLM_TEST_AGENT_MODEL}")
        set_tests_properties(iiLocalLLM.installed_skills_catalog_inference PROPERTIES TIMEOUT 600)
    endif()
endif()
if(IILOCALLLM_TEST_CHAT_GGUF)
    add_executable(iiLocalLLMReasoningConsumer ../reasoning_runtime_smoke.cpp)
    target_link_libraries(iiLocalLLMReasoningConsumer PRIVATE iiLocalLLM::iiLocalLLM)
    add_test(NAME iiLocalLLM.installed_reasoning_template COMMAND iiLocalLLMReasoningConsumer "${IILOCALLLM_TEST_CHAT_GGUF}")
    set_tests_properties(iiLocalLLM.installed_reasoning_template PROPERTIES TIMEOUT 90)
    if(IILOCALLLM_TEST_MCP_PYTHON)
        add_executable(iiLocalLLMDiscoveryInferenceConsumer ../agent_runtime_smoke.cpp)
        target_link_libraries(iiLocalLLMDiscoveryInferenceConsumer PRIVATE iiLocalLLM::iiLocalLLM)
        add_test(NAME iiLocalLLM.installed_configured_mcp_inference COMMAND iiLocalLLMDiscoveryInferenceConsumer "${IILOCALLLM_TEST_CHAT_GGUF}"
            "${IILOCALLLM_TEST_MCP_PYTHON}" "${CMAKE_CURRENT_SOURCE_DIR}/../mcp_official_server.py" --configured-eager)
        set_tests_properties(iiLocalLLM.installed_configured_mcp_inference PROPERTIES TIMEOUT 180)
        if(UNIX AND NOT IOS AND NOT ANDROID AND NOT EMSCRIPTEN)
            add_test(NAME iiLocalLLM.installed_mcp_output_hook_inference COMMAND iiLocalLLMDiscoveryInferenceConsumer "${IILOCALLLM_TEST_CHAT_GGUF}"
                "${IILOCALLLM_TEST_MCP_PYTHON}" "${CMAKE_CURRENT_SOURCE_DIR}/../mcp_official_server.py" --mcp-output-hook)
            set_tests_properties(iiLocalLLM.installed_mcp_output_hook_inference PROPERTIES TIMEOUT 180 LABELS "inference")
        endif()
        add_test(NAME iiLocalLLM.installed_discovery_inference COMMAND iiLocalLLMDiscoveryInferenceConsumer "${IILOCALLLM_TEST_CHAT_GGUF}"
            "${IILOCALLLM_TEST_MCP_PYTHON}" "${CMAKE_CURRENT_SOURCE_DIR}/../mcp_official_server.py" --discovery)
        set_tests_properties(iiLocalLLM.installed_discovery_inference PROPERTIES TIMEOUT 180)
        if(IILOCALLLM_TEST_AGENT_CATALOG AND IILOCALLLM_TEST_AGENT_THINKING_CONTROL)
            add_test(NAME iiLocalLLM.installed_discovery_thinking_control COMMAND iiLocalLLMDiscoveryInferenceConsumer --catalog
                "${IILOCALLLM_TEST_AGENT_CATALOG}" "${IILOCALLLM_TEST_AGENT_MODEL}" "${IILOCALLLM_TEST_MCP_PYTHON}"
                "${CMAKE_CURRENT_SOURCE_DIR}/../mcp_official_server.py" --thinking-control)
            set_tests_properties(iiLocalLLM.installed_discovery_thinking_control PROPERTIES TIMEOUT 600)
        endif()
    endif()
    add_executable(iiLocalLLMCompactionInferenceConsumer ../compaction_runtime_smoke.cpp)
    target_link_libraries(iiLocalLLMCompactionInferenceConsumer PRIVATE iiLocalLLM::iiLocalLLM)
    add_test(NAME iiLocalLLM.installed_compaction_inference COMMAND iiLocalLLMCompactionInferenceConsumer "${IILOCALLLM_TEST_CHAT_GGUF}")
    set_tests_properties(iiLocalLLM.installed_compaction_inference PROPERTIES TIMEOUT 240)
endif()
if(IILOCALLLM_TEST_MCP_PYTHON)
    add_executable(iiLocalLLMMcpConsumer ../mcp_official_smoke.cpp)
    target_link_libraries(iiLocalLLMMcpConsumer PRIVATE iiLocalLLM::iiLocalLLM)
    add_test(NAME iiLocalLLM.installed_mcp_consumer COMMAND iiLocalLLMMcpConsumer
        "${IILOCALLLM_TEST_MCP_PYTHON}" "${CMAKE_CURRENT_SOURCE_DIR}/../mcp_official_server.py")
    add_test(NAME iiLocalLLM.installed_mcp_http_consumer COMMAND iiLocalLLMMcpConsumer
        "${IILOCALLLM_TEST_MCP_PYTHON}" "${CMAKE_CURRENT_SOURCE_DIR}/../mcp_official_server.py" streamable-http)
endif()
file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/tmp")
get_property(_consumer_tests DIRECTORY PROPERTY TESTS)
set_tests_properties(${_consumer_tests} PROPERTIES ENVIRONMENT_MODIFICATION
    "TMPDIR=set:${CMAKE_CURRENT_BINARY_DIR}/tmp;TMP=set:${CMAKE_CURRENT_BINARY_DIR}/tmp;TEMP=set:${CMAKE_CURRENT_BINARY_DIR}/tmp;IILOCALLLM_APP_ENDPOINTS=set:${CMAKE_CURRENT_BINARY_DIR}/tmp/app-endpoints")
